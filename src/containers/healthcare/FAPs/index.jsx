import React from 'react'
import Form from '../../../components/forms/Form'
import { FORM_FAPS } from '../../../utils/constants/forms/formHealth'

const FAPs = () => {
   return <Form dataForm={FORM_FAPS} />
}

export default FAPs
